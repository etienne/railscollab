#people
  = render :partial => 'companies/people_list', :collection => @project_companies

- if @project.can_be_managed_by(@logged_user)
  .hint= :permissions_info.l_with_args(:link => link_to(:permissions_form.l, permissions_project_path(:id => @active_project.id)))